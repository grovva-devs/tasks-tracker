node_modules
dist
.next
.env
.env.local
.env.*.local
.turbo
*.tsbuildinfo
coverage
.DS_Store
.turbo
drizzle/*.sql  # migrations — track these in git
drizzle/meta/  # meta — track this too
!drizzle/       # but track the drizzle folder with migrations